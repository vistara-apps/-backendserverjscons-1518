# Backend Environment Variables
PORT=5000
JWT_SECRET=campus_connect_secret_key_2024_change_in_production
NODE_ENV=development

# Database Configuration (for future use)
# DATABASE_URL=postgresql://username:password@localhost:5432/campusconnect
# MONGODB_URI=mongodb://localhost:27017/campusconnect

# Email Configuration (for future use)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# File Upload Configuration (for future use)
# UPLOAD_MAX_SIZE=10485760
# UPLOAD_ALLOWED_TYPES=jpg,jpeg,png,pdf,doc,docx
